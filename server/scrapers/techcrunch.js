// TechCrunch scraper (articles + Crunchbase deal announcements)
// Uses: axios + cheerio
// Fallback to RSS feed where possible to avoid scraping
// TODO: implement in Phase 1 (RSS) / Phase 2 (deal scraper)

module.exports = {
  scrapeArticles: async () => { throw new Error('not implemented') },
  scrapeDeals:    async () => { throw new Error('not implemented') },
}
